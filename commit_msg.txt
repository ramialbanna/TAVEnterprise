TAV-WF P9 + TAV-BB OPEN-5: retire Classic UI and ship YMM-first MaxBuy

P9 — Classic UI retired (WF-5):
- Remove Classic/New toggle; interface-provider always returns 'new'
- Delete sidebar-nav-classic.tsx, interface-toggle.tsx, opportunities-client-classic.tsx
- Simplify all Classic forks in app-shell, opportunities, dashboard components
- Remove unnecessary kpis/historicalSales fetches from dashboard page
- Drop NEXT_PUBLIC_CLASSIC_UI_ENABLED from .env.example
- MAXBUY_EVALUATE_ENABLED=true already set in staging/production wrangler.toml

OPEN-5 — YMM-first MaxBuy (no VIN required):
- Migration 0059: maxbuy_lookups.vin nullable + year/make/model columns;
  maxbuy_evaluated_passes same; identity CHECK enforces VIN-or-YMM
- API schema: vin optional, year+make+model accepted as first-class path
- evaluateRun: VIN and YMM branches; YMM goes direct to lookupMmrByYmm
- Scoring: vinAbsent flag caps verdict at REVIEW, adds ymm_primary_no_vin reason
- Gates: vin optional in GateInput; title gates skipped without VIN
- Persistence: nullable vin + ymm fields throughout lookups and passes
- Background evaluate fires on YMM fields after manual submit when no VIN
- Form: YMM fields (year/make/model/trim) appear when VIN is blank
- Embedded deal detail pre-fills YMM from listing; awaiting_vin gate removed
- /maxbuy page and descriptions updated
